import styled from 'styled-components';

export const _WelcomeContent = styled.div`
  width: 100%;
  max-width: 30rem;
  padding: 0 ${({ theme }) => theme.spacers.md};
  box-sizing: border-box;
  margin: 0 auto;
`;
